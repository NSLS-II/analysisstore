{
"properties": {
        "data": {
            "type": "object",
            "description": "The actual analysis data"
        },
        "timestamps": {
            "type": "object",
            "description": "The timestamps of the individual analysis data"
        },
        “data_reference_header”: {
            "type": "string",
            "description": "UID to point back to DataReferenceHeader for this event stream"
        },
        "seq_num": {
            "type": "integer",
            "description": "Sequence number to identify the location of this DataReference in the DataReference stream"
        },
        "time": {
            "type": "number",
            "description": "The event time.  This maybe different than the timestamps on each of the data entries"
        },
        "uid": {
            "type": "string",
            "description": "Globally unique identifier for this DataReference”
        }
    },
    "required": [
        "uid",
        "data",
        "timestamps",
        "time",
        "descriptor",
        "seq_num"
    ],
    "additionalProperties": false,
    "type": "object",
    "title": “data_reference”,
    "description": "Document to record a quanta of analyzed data"
 }
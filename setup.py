from setuptools import setup

setup(name='analysisbucket',
      version='0.0.0',
      author='Brookhaven National Laboratory',
      py_modules=['analysisbucket'],
      description='',
      requires=['six']
      )
